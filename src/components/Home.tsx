import React from "react";

const Home = () => (
  <div className="jumbo">
    <section className="container">
      <h1 className="my-5">🚧 Work in progress</h1>
    </section>
  </div>
);

export default Home;
